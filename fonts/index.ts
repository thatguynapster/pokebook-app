import local from "next/font/local";

export const clash = local({
  src: "../fonts/ClashDisplay-Variable.woff2",
  variable: "--font-clash",
});

export const general_sans = local({
  src: "../fonts/GeneralSans-Variable.woff2",
  variable: "--font-general-sans",
});
